validation:
    nullPwd: Password should not be null.
    success: Password is accepted.
    rules:
         special : [^A-Za-z0-9]
         letters: ^[0-9]*$
         numbers: \\b[^\\d\\W]+\\b
         sequence: (\\w+?)\\1
         length: ^(?!.*\b\w{7,}\b).+$
         upperCase: "[A-Z]+"
    errorMessages:
                 sequence :Password must not contain any sequence of characters immediately followed by the same sequence.
                 special : Password must consist of a mixture of lowercase letters and numerical digits only, with at least one of each.
                 upperCase  : Password must consist of a mixture of lowercase letters and numerical digits only, with at least one of each.
                 numbers : Password must consist of a mixture of lowercase letters and numerical digits only, with at least one of each.
                 letters: Password must consist of a mixture of lowercase letters and numerical digits only, with at least one of each.
                 length: Password must be between 5 and 12 characters in length.